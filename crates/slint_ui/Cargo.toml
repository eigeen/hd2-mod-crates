[package]
name = "mod_armor_migrator_slint_ui"
version = "0.1.0"
edition = "2021"
description = "Slint desktop UI prototype for mod_armor_migrator"
license = "MIT OR Apache-2.0"

[dependencies]
eyre = "0.6.12"
mod_armor_migrator = { path = "../.." }
rfd = "0.17.2"
slint = "1.16.1"
tracing-subscriber = { version = "0.3.23", features = ["env-filter"] }
url = "2.5.7"

[build-dependencies]
slint-build = "1.16.1"
